# 🏗️ Architecture Documentation

## System Overview

```
┌─────────────────────────────────────────────────────────────┐
│                     Expense Tracker Bot                      │
│                    Architecture Diagram                      │
└─────────────────────────────────────────────────────────────┘

┌──────────────┐
│ Telegram User│
│   (Mobile)   │
└──────┬───────┘
       │
       │ Messages & Commands
       │
       ▼
┌─────────────────────────────────────────────────────────────┐
│                        BOT LAYER                             │
│  ┌────────────────────────────────────────────────────────┐ │
│  │              bot.py (Main Entry Point)                 │ │
│  │  • Initialize aiogram Bot & Dispatcher                 │ │
│  │  • Register handlers and middleware                    │ │
│  │  • Start polling for updates                           │ │
│  │  • Logging and error handling                          │ │
│  └───────────────────┬────────────────────────────────────┘ │
│                      │                                       │
│                      ▼                                       │
│  ┌────────────────────────────────────────────────────────┐ │
│  │           Access Control Middleware                    │ │
│  │  • Check user ID against whitelist                     │ │
│  │  • Reject unauthorized users silently                  │ │
│  │  • Log access attempts                                 │ │
│  └───────────────────┬────────────────────────────────────┘ │
└────────────────────────────────────────────────────────────┘
                       │
                       ▼
┌─────────────────────────────────────────────────────────────┐
│                     HANDLER LAYER                            │
│  ┌────────────────────────────────────────────────────────┐ │
│  │              handlers.py (Command Handlers)            │ │
│  │                                                        │ │
│  │  Commands:                                             │ │
│  │  • /start  → Welcome message                          │ │
│  │  • /stats  → Show statistics                          │ │
│  │  • /categories → List categories                      │ │
│  │  • /help   → Usage instructions                       │ │
│  │                                                        │ │
│  │  Message Processing:                                   │ │
│  │  • Parse expense input                                │ │
│  │  • Handle FSM states (category selection)            │ │
│  │  • Validate and format data                           │ │
│  │  • Send responses to user                             │ │
│  └───────────────────┬────────────────────────────────────┘ │
└────────────────────────────────────────────────────────────┘
                       │
                       ▼
┌─────────────────────────────────────────────────────────────┐
│                   VALIDATION LAYER                           │
│  ┌────────────────────────────────────────────────────────┐ │
│  │           validators.py (Pydantic Models)              │ │
│  │                                                        │ │
│  │  ExpenseInput:                                         │ │
│  │  • Validate category (string, lowercase)              │ │
│  │  • Validate amount (float, positive)                  │ │
│  │  • Validate comment (optional string)                 │ │
│  │  • Add timestamp and user ID                          │ │
│  │                                                        │ │
│  │  ParsedMessage:                                        │ │
│  │  • Parse "category amount comment"                    │ │
│  │  • Parse "amount" only                                │ │
│  │  • Handle various input formats                       │ │
│  └───────────────────┬────────────────────────────────────┘ │
└────────────────────────────────────────────────────────────┘
                       │
                       ▼
┌─────────────────────────────────────────────────────────────┐
│                   INTEGRATION LAYER                          │
│  ┌────────────────────────────────────────────────────────┐ │
│  │        google_service.py (Google Sheets API)           │ │
│  │                                                        │ │
│  │  Connection:                                           │ │
│  │  • Authenticate with service account                  │ │
│  │  • Open/create spreadsheet                            │ │
│  │  • Initialize header row                              │ │
│  │                                                        │ │
│  │  Operations:                                           │ │
│  │  • add_expense() → Append row to sheet               │ │
│  │  • get_statistics() → Calculate totals               │ │
│  │  • get_categories() → List unique categories         │ │
│  │  • get_records_by_date_range() → Filter data         │ │
│  └───────────────────┬────────────────────────────────────┘ │
└────────────────────────────────────────────────────────────┘
                       │
                       ▼
┌─────────────────────────────────────────────────────────────┐
│                      DATA LAYER                              │
│  ┌────────────────────────────────────────────────────────┐ │
│  │              Google Sheets (Database)                  │ │
│  │                                                        │ │
│  │  Columns:                                              │ │
│  │  ┌──────┬──────────┬────────┬─────────┬─────────┐    │ │
│  │  │ Date │ Category │ Amount │ Comment │ User ID │    │ │
│  │  ├──────┼──────────┼────────┼─────────┼─────────┤    │ │
│  │  │ ...  │   ...    │  ...   │   ...   │   ...   │    │ │
│  │  └──────┴──────────┴────────┴─────────┴─────────┘    │ │
│  └────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                   CONFIGURATION LAYER                        │
│  ┌────────────────────────────────────────────────────────┐ │
│  │              config.py (Settings)                      │ │
│  │                                                        │ │
│  │  • Load environment variables from .env               │ │
│  │  • Validate configuration                             │ │
│  │  • Manage whitelist (ALLOWED_USERS)                   │ │
│  │  • Define default categories                          │ │
│  │  • Provide Google Sheets credentials path             │ │
│  └────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
```

## Data Flow

### 1. Adding an Expense

```
User sends: "food 2500 coffee"
    │
    ▼
[Bot receives message]
    │
    ▼
[Access Control Middleware]
    │ Check if user_id in ALLOWED_USERS
    ├─ NO → Ignore silently
    └─ YES → Continue
        │
        ▼
[Handler: process_expense()]
    │
    ▼
[ParsedMessage.parse_from_text()]
    │ Parse: category="food", amount=2500, comment="coffee"
    │
    ▼
[ExpenseInput validation]
    │ Validate and normalize data
    │ Add timestamp and user_id
    │
    ▼
[GoogleSheetsService.add_expense()]
    │ Convert to row format
    │ Append to spreadsheet
    │
    ▼
[Google Sheets]
    │ Row added: [2024-12-08 17:30, food, 2500, coffee, 123456789]
    │
    ▼
[Send confirmation to user]
    │
    ▼
User sees: "✅ Expense saved!"
```

### 2. Viewing Statistics

```
User sends: "/stats"
    │
    ▼
[Handler: cmd_stats()]
    │
    ▼
[GoogleSheetsService.get_statistics('today', user_id)]
[GoogleSheetsService.get_statistics('week', user_id)]
[GoogleSheetsService.get_statistics('month', user_id)]
    │
    ▼
[Get all records from sheet]
    │
    ▼
[Filter by date range and user_id]
    │
    ▼
[Calculate totals and group by category]
    │
    ▼
[Format statistics message]
    │
    ▼
User sees: "📊 Your Expense Statistics..."
```

## Component Interactions

```
┌──────────┐     uses      ┌──────────┐
│  bot.py  │ ────────────> │handlers.py│
└──────────┘               └─────┬─────┘
                                 │ uses
                                 ▼
                           ┌─────────────┐
                           │validators.py│
                           └─────────────┘
                                 │ uses
                                 ▼
                        ┌──────────────────┐
                        │google_service.py │
                        └────────┬─────────┘
                                 │ uses
                                 ▼
                           ┌──────────┐
                           │config.py │
                           └──────────┘
                                 │ loads
                                 ▼
                              ┌──────┐
                              │ .env │
                              └──────┘
```

## State Management

### FSM (Finite State Machine) Flow

```
User sends only amount: "2500"
    │
    ▼
[State: None]
    │
    ▼
[Set state: waiting_for_category]
[Store amount in state data]
    │
    ▼
Bot asks: "Please send the category"
    │
    ▼
User sends: "food"
    │
    ▼
[State: waiting_for_category]
    │
    ▼
[Retrieve amount from state]
[Create ExpenseInput with category + amount]
    │
    ▼
[Clear state]
    │
    ▼
[Save to Google Sheets]
    │
    ▼
Bot confirms: "✅ Expense saved!"
```

## Security Architecture

```
┌─────────────────────────────────────────────────────────┐
│                   Security Layers                        │
└─────────────────────────────────────────────────────────┘

Layer 1: Access Control
┌──────────────────────────────────────────────────────────┐
│ Middleware checks user_id against ALLOWED_USERS          │
│ • Authorized → Process message                           │
│ • Unauthorized → Silent rejection (no response)          │
└──────────────────────────────────────────────────────────┘

Layer 2: Environment Variables
┌──────────────────────────────────────────────────────────┐
│ Sensitive data in .env (not committed to git)            │
│ • TELEGRAM_TOKEN                                         │
│ • ALLOWED_USERS                                          │
│ • Google credentials path                                │
└──────────────────────────────────────────────────────────┘

Layer 3: Service Account
┌──────────────────────────────────────────────────────────┐
│ Google Sheets access via service account                 │
│ • No OAuth flow needed                                   │
│ • Credentials in credentials.json (gitignored)           │
│ • Sheet shared only with service account email           │
└──────────────────────────────────────────────────────────┘

Layer 4: Input Validation
┌──────────────────────────────────────────────────────────┐
│ Pydantic models validate all input                       │
│ • Type checking                                          │
│ • Range validation (amount > 0)                          │
│ • String normalization                                   │
└──────────────────────────────────────────────────────────┘
```

## Error Handling Flow

```
┌─────────────────┐
│  User Input     │
└────────┬────────┘
         │
         ▼
    ┌────────────┐
    │ Try Parse  │
    └─────┬──────┘
          │
    ┌─────┴─────┐
    │           │
    ▼           ▼
 Success     ValueError
    │           │
    │           └──> Send error message
    │                "❌ Invalid format..."
    ▼
┌────────────┐
│ Try Validate│
└─────┬──────┘
      │
  ┌───┴───┐
  │       │
  ▼       ▼
Success  ValidationError
  │       │
  │       └──> Send error message
  │            "❌ Invalid amount..."
  ▼
┌────────────┐
│ Try Save   │
└─────┬──────┘
      │
  ┌───┴───┐
  │       │
  ▼       ▼
Success  Exception
  │       │
  │       └──> Send error message
  │            "❌ Failed to save..."
  │            Log error
  ▼
Send success message
"✅ Expense saved!"
```

## Deployment Architecture

### Local Development
```
┌──────────────┐
│ Developer PC │
│              │
│  ┌────────┐  │
│  │  Bot   │  │
│  │Process │  │
│  └────────┘  │
└──────────────┘
       │
       ├──> Telegram API
       └──> Google Sheets API
```

### Production (Server)
```
┌─────────────────────┐
│   Linux Server      │
│                     │
│  ┌──────────────┐   │
│  │   systemd    │   │
│  │   service    │   │
│  │              │   │
│  │  ┌────────┐  │   │
│  │  │  Bot   │  │   │
│  │  │Process │  │   │
│  │  └────────┘  │   │
│  └──────────────┘   │
│                     │
│  • Auto-restart     │
│  • Logging          │
│  • Background run   │
└─────────────────────┘
```

### Docker Deployment
```
┌─────────────────────┐
│  Docker Container   │
│                     │
│  ┌──────────────┐   │
│  │ Python 3.11  │   │
│  │              │   │
│  │  ┌────────┐  │   │
│  │  │  Bot   │  │   │
│  │  │Process │  │   │
│  │  └────────┘  │   │
│  └──────────────┘   │
│                     │
│  Volumes:           │
│  • .env             │
│  • credentials.json │
└─────────────────────┘
```

## Performance Considerations

### Polling Strategy
```
Bot uses long polling:
• Efficient for low-traffic bots
• No webhook setup needed
• Works behind NAT/firewall
• Suitable for personal use
```

### Google Sheets API
```
Rate Limits:
• 100 requests per 100 seconds per user
• 500 requests per 100 seconds per project

Optimization:
• Batch reads when possible
• Cache categories list
• Minimize API calls
```

### Memory Usage
```
Lightweight application:
• ~50MB RAM typical usage
• No database overhead
• Minimal dependencies
• Suitable for small VPS
```

## Scalability

### Current Design (1-10 users)
```
✅ Perfect for personal/family use
✅ Google Sheets as database
✅ Simple deployment
✅ Low resource usage
```

### Future Scaling (10-100 users)
```
Consider:
• Add caching layer (Redis)
• Batch Google Sheets writes
• Add rate limiting per user
• Monitor API quotas
```

### Enterprise Scale (100+ users)
```
Would need:
• Replace Google Sheets with PostgreSQL
• Add message queue (RabbitMQ)
• Implement webhooks instead of polling
• Add load balancer
• Horizontal scaling
```

## Technology Choices

### Why aiogram 3.x?
```
✅ Modern async/await syntax
✅ Built-in FSM support
✅ Type hints throughout
✅ Active development
✅ Excellent documentation
```

### Why Google Sheets?
```
✅ No database setup needed
✅ Visual interface for data
✅ Easy to export/backup
✅ Familiar to users
✅ Free tier sufficient
```

### Why Pydantic?
```
✅ Runtime type validation
✅ Data parsing
✅ Clear error messages
✅ IDE support
✅ Industry standard
```

## Monitoring & Logging

### Logging Levels
```
INFO:  Bot startup, shutdown, user actions
WARNING: Unauthorized access attempts
ERROR: API failures, validation errors
```

### What Gets Logged
```
✅ Bot startup/shutdown
✅ User commands
✅ Expense additions
✅ Errors and exceptions
✅ Unauthorized access attempts
❌ Sensitive data (tokens, user IDs in messages)
```

---

**This architecture supports:**
- 🎯 Simple deployment
- 🔒 Secure operation
- 📊 Reliable data storage
- 🚀 Easy maintenance
- 📈 Future scalability
